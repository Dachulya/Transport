package Transport;

public class Bus extends Transport{
    public Bus(String color, String model, String brend, int maxSpeed, int dateRelease, String country) {
        super(color, model, brend, maxSpeed, dateRelease, country);
    }


}
